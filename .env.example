# cala.studio · variables de entorno del frontend
# Copia este archivo a `.env` y rellena los valores (el .env NO se sube a git).

# Supabase — panel del proyecto → Project Settings → API
# Son PÚBLICAS y seguras en el frontend: la seguridad la da RLS + las RPC.
VITE_SUPABASE_URL=https://TU-PROYECTO.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...

# (Contacto) Formspree — ya en uso en el formulario de preguntas
# VITE_FORMSPREE_ID=xnjkglwj
